[build]
builder = "dockerfile"

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

# 全栈服务配置（包含 Web + API + Worker）
[[services]]
name = "relax-git-fullstack"
source = "."
dockerfile = "Dockerfile"
