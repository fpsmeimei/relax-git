# pnpm 配置文件
# 确保 monorepo 依赖正确处理

# 使用隔离的 node_modules 结构，规避符号链接缺失问题
node-linker=isolated

# 严格的对等依赖处理
strict-peer-dependencies=false

# 缓存配置
store-dir=~/.pnpm-store

# 注册表配置
registry=https://registry.npmjs.org/

# 安全配置
audit-level=moderate
