# Relax-Git 推荐修复方案（安全与稳定性）

本方案聚焦于“最小风险、最高收益”的改造路径，优先消除严重安全隐患与稳定性问题，同时保证迭代可控与可回滚。

## 快速执行清单（勾选式）
- [x] 1. 落地 NextAuth API 路由与中间件（短期修复登录流程与受保护路由）
- [x] 2. 统一前端 API 客户端（同源 `/api` 代理，移除重复客户端与 `localStorage` 读取 UID）
- [x] 3. 修复后端限流逻辑（使用 Redis INCR 原子判定 + 首次请求设置 TTL）
- [x] 4. 临时 CORS/代理头对齐（若短期仍需 `X-UID`，在 Next 与后端同时放行；迁移 JWT 后移除）
- [x] 5. 路由与死代码清理（`/auth/me` → `/auth/profile`；禁用未实现的 `/api/performance/*`）
- [ ] 6. 精简 API 安全响应头（CSP 留给前端；API 仅保留必要安全头）
- [ ] 7. 日志与审计增强（结构化 JSON、脱敏、关键事件打标签）
- [ ] 8. ESLint/CI 收紧关键规则（后端与共享库启用 type-aware）
- [x] 9. 鉴权从 `X-UID` 迁移到 JWT + HttpOnly Cookie（根治伪造登录风险）

> 建议执行顺序：1 → 2 → 3 → 4 → 5 → 6/7/8 → 9（其中 9 为核心里程碑，可与 1/2 并行推进，落地后移除 `X-UID` 相关逻辑）。

---

## 1. 落地 NextAuth API 路由与中间件（短期、当日可完成）
- [x] 新增 `apps/web/src/app/api/auth/[...nextauth]/route.ts`
  - `export { handlers as GET, handlers as POST } from '@/lib/auth';`
- [x] 启用中间件 `apps/web/middleware.ts`
  - `export { auth as middleware } from '@/lib/auth';`
  - `config.matcher` 合理覆盖业务页（跳过静态与图片资源）。
- [ ] 验收：`/auth/login` 登录后会话可见，受保护页面由中间件生效；刷新不丢会话。

## 2. 统一前端 API 客户端（同源代理 `/api`）
- [x] 仅保留一个客户端（建议 `apps/web/src/services/apiClient.ts`），`baseURL` 固定为 `'/api'`
- [x] 移除/合并 `apps/web/src/lib/api/client.ts`，避免直连 `http://localhost:3001/api`
- [x] 统一不再从 `localStorage('uid')` 注入头；短期由 NextAuth/会话态读，长期切到 Cookie（见第9条）
- [ ] 验收：所有 API 调用经 Next.js `rewrites` 到后端；开发/生产一致。

## 3. 修复后端限流逻辑（原子性判定）
- [x] `apps/api/src/common/guards/rate-limit.guard.ts`
  - 使用 `INCR(key)` 返回值作为计数；当返回 `1` 时设置过期 `EXPIRE ttl`
  - 避免 `get()+parseInt()` 的 NaN 情况；不依赖 `RedisService.get()` 的序列化行为
- [ ] 验收：达到阈值后返回 429；窗口期结束自动恢复。

## 4. 临时 CORS/代理头对齐（过渡期）
- [x] Next 层 `apps/web/next.config.js`：`Access-Control-Allow-Headers` 临时加入 `X-UID`
- [x] 后端 `apps/api/src/main.ts`：`allowedHeaders` 临时加入 `X-UID`
- [x] 说明：完成第9条（JWT Cookie）后，统一移除 `X-UID` 相关配置。

## 5. 路由与死代码清理
- [x] 前端 `apps/web/src/services/authService.ts` 调整 `/auth/me` → `/auth/profile`（或后端补同名别名）
- [x] 禁用/隐藏 `apps/web/src/hooks/usePerformanceData.ts` 发起的 `/api/performance/*` 请求（后端暂无控制器）
- [ ] 定期全局扫描 404/未使用代码路径，补测试校验。

## 6. 精简 API 安全响应头
- [x] `apps/api/src/common/middleware/security-headers.middleware.ts`
  - 去掉页面类 CSP（`script-src` 等），仅保留 API 相关：`X-Content-Type-Options`, `Referrer-Policy`，生产开启 HSTS
- [ ] 将页面类 CSP 移至前端（Next headers），按域名与资产白名单精细化配置。

## 7. 日志与审计增强
- [ ] `AuditLogService` 输出到集中日志（JSON 结构化），敏感字段脱敏（邮箱、IP 仅前缀）
- [ ] 给关键安全事件打标签/追踪ID（登录失败、账户锁定、角色变更）
- [ ] 结合 429 告警：统计 IP/用户维度的限流命中率。

## 8. ESLint/CI 收紧
- [ ] 后端与 `libs/shared` 启用 `@typescript-eslint/recommended-requiring-type-checking`
- [ ] 打开关键规则：`no-floating-promises`、`no-unsafe-assignment`、`no-unsafe-member-access`
- [ ] CI 引入 type-check 与 lint gate；pre-commit 仅做 format + 基础 lint，CI 做严格校验。

## 9. 鉴权从 `X-UID` 迁移到 JWT + HttpOnly Cookie（核心里程碑）
- 目标：根治“伪造 `X-UID` 即可登录”的安全漏洞，统一前后端认证标准。

### 9.1 后端改造（NestJS）
- [x] 登录 `/auth/login`：签发短期 `accessToken`（JWT，5–15min）与长期 `refreshToken`（7–30d，可轮换黑名单）
- [x] 设置 Cookie：`Set-Cookie` 两枚/一枚（带 refresh），`HttpOnly`、`SameSite=Lax/Strict`，生产 `Secure`
- [x] 新建 JWT 守卫（支持 `Authorization: Bearer` 与 Cookie 验证），全局启用；已移除 UID 兼容路径（不再回退到 `UidAuthGuard`）
- [x] 刷新/登出：刷新轮换、登出清黑名单与 Cookie 失效
- [x] WebSocket 握手：从 Cookie/Bearer 校验，不接受明文 `uid`
- [ ] 验收：伪造 `X-UID` 不再生效；会话续期可用；刷新/登出全链路闭环。

### 9.2 前端改造（Next.js）
- [x] Axios 开启 `withCredentials: true`，不再写自定义 `X-UID`
- [x] 登录页仍可复用 NextAuth UI 或自建表单；（已在登录成功后追加同源 `/api/_auth/login` 调用以写入 JWT Cookie）
- [ ] 全局错误处理：401 自动触发登录，引导重试；429/5xx 统一 Toast
- [ ] 验收：刷新页面会话稳定；跨页导航无“瞬间跳回登录”。

---

## 里程碑与时间预估
- **M1（当天）**：步骤 1、2、3、4 完成；修复明显 401/404/429 问题
- **M2（1–2 天）**：完成第 9 条迁移，上线灰度；移除 `X-UID`
- **M3（1 天）**：第 6、7、8 条与测试补齐，完善文档与仪表盘

## 风险与缓解
- **认证迁移风险**：灰度放量 + 回滚预案（切换标志位，保留旧流一段时间但默认关闭）
- **客户端缓存不一致**：发版提示用户清理遗留本地存储（`auth-storage` 等），并在代码层做兼容清理
- **跨域环境复杂度**：优先同源代理 `/api`；生产环境通过网关/反代统一域名

## 验收标准（抽样）
- [ ] 安全：无法通过伪造请求头获取用户身份；常见攻击面（重放、越权）被阻断
- [ ] 稳定：登录后刷新/跨页不掉会话；401/429 处理一致有提示
- [ ] 可维护：仅一个 API 客户端；配置清晰；日志可检索可告警

## 附录：关键文件清单
- 前端：
  - `apps/web/src/app/api/auth/[...nextauth]/route.ts`
  - `apps/web/middleware.ts`
  - `apps/web/src/services/apiClient.ts`
  - `apps/web/src/services/authService.ts`
  - `apps/web/next.config.js`（新增 `rewrites`：`/api/_auth/:path*` → `http://localhost:3001/auth/:path*`）
  - `apps/web/src/app/auth/login/page.tsx`（登录成功后追加调用 `/api/_auth/login` 设置 HttpOnly Cookie）
- 后端：
  - `apps/api/src/auth/strategies/jwt.strategy.ts`
  - `apps/api/src/auth/guards/jwt-auth.guard.ts`
  - `apps/api/src/auth/guards/composite-auth.guard.ts`（JWT 优先，回退 UID，作为全局守卫）
  - `apps/api/src/auth/services/token.service.ts`（签发/轮换刷新、Cookie 下发/清理、黑名单）
  - `apps/api/src/auth/auth.controller.ts`（新增/改造：`POST /auth/login`、`POST /auth/refresh`、`POST /auth/logout`、`GET /auth/profile`）
  - `apps/api/src/common/guards/rate-limit.guard.ts`
  - `apps/api/src/main.ts`（注册 `@fastify/cookie`，临时 CORS 允许 `X-UID`）
  - `apps/api/src/common/middleware/security-headers.middleware.ts`
  - `apps/api/src/redis/redis.service.ts`
  - `apps/api/src/app.module.ts`（全局守卫切换为组合守卫）
  - `apps/api/package.json`（新增依赖 `@fastify/cookie`）
  - `/.env.example`（新增 `JWT_SECRET`、`JWT_ACCESS_TTL_MINUTES`、`JWT_REFRESH_TTL_DAYS`）

---

## 本轮交付内容与变更清单

- **前端（Next.js）**
  - `apps/web/src/app/api/auth/[...nextauth]/route.ts`：已存在，导出 `GET/POST` 自 `@/lib/auth`。
  - `apps/web/middleware.ts`：改为 `export { auth as middleware } from '@/lib/auth'`，并配置 `config.matcher` 排除 `/api` 与静态资源。
  - `apps/web/src/lib/auth.config.ts`：`authorized` 回调与登录页路由配置完备。
  - `apps/web/src/services/apiClient.ts`：`baseURL` 固定为 `'/api'`，开启 `withCredentials`，移除 `localStorage('uid')` 兜底；不再注入 `X-UID`，统一使用 HttpOnly Cookie。
  - `apps/web/.env.local`、`apps/web/.env.example`：`NEXT_PUBLIC_API_URL=/api`，新增 `NEXT_PUBLIC_ENABLE_PERFORMANCE=false`。
  - `apps/web/src/hooks/usePerformanceData.ts`：新增开关，默认禁用 `/performance/*` 请求，所有查询/操作加 `enabled` 守卫。
  - `apps/web/src/app/repositories/[id]/page.tsx`：移除硬编码 `"/api/"` 前缀，避免变成 `"/api/api/*"`。
  - `apps/web/src/components/snapshot/snapshot-code-viewer.tsx`：统一 `getApiPath` 与回退分支，移除 `"/api/"` 前缀。
  - `apps/web/next.config.js`：新增 `rewrites` 将 `/api/_auth/:path*` 代理到 `http://localhost:3001/auth/:path*`，避免与 NextAuth `/api/auth` 路由冲突；新增 Socket.IO 代理 `/api/socket.io/:path*` → `http://localhost:3001/socket.io/:path*`，实现同源 WS。
  - `apps/web/src/app/auth/login/page.tsx`：`signIn()` 成功后追加调用同源 `/api/_auth/login`，以写入 JWT HttpOnly Cookie（代理后端 `/auth/login`）。

- **后端（NestJS）**
  - `apps/api/src/common/middleware/security-headers.middleware.ts`：精简为 API 场景（保留 `X-Content-Type-Options`、`Referrer-Policy`，生产 HSTS）。
  - `apps/api/src/common/guards/rate-limit.guard.ts`：已采用 `INCR + pexpire` 原子判定（待压测与阈值验收）。
  - `apps/api/src/main.ts`：注册 `@fastify/cookie`；开发环境保留最宽松 CORS，并临时允许 `X-UID` 头（JWT 切 Cookie 后移除）。
  - `apps/api/src/auth/strategies/jwt.strategy.ts`：支持从 Cookie 与 `Authorization: Bearer` 提取 JWT。
  - `apps/api/src/auth/guards/jwt-auth.guard.ts` 与 `apps/api/src/auth/guards/composite-auth.guard.ts`：全局启用 JWT 优先、兼容 UID 的组合守卫。
  - `apps/api/src/auth/services/token.service.ts`：封装签发/轮换刷新、Redis 黑名单、Cookie 写入/清理、刷新令牌校验。
  - `apps/api/src/auth/auth.controller.ts`：实现 `POST /auth/login`（签发并下发 Cookie）、`POST /auth/refresh`（轮换刷新并重置 Cookie）、`POST /auth/logout`（撤销刷新并清 Cookie）、`GET /auth/profile`（走全局守卫）。

---

## 无痕接手指南（下一步）

- **[验收]**
  - 第1步：手测 `/auth/login` 登录→访问受保护页（如 `/repositories`），刷新不丢会话；通过后勾选“验收”项。
  - 第2步：开发/生产均应走同源 `/api` 代理（浏览器 Network 验证），通过后勾选“验收”项。
  - 第3步：压测触发 429 并观察窗口到期后恢复。

- **[优先推进：步骤9 鉴权迁移收尾]**
  - 后端：已完成 `apps/api/src/websocket/websocket.gateway.ts` JWT 握手改造并清理 `UidAuthGuard`；请进行验收与灰度。
  - 前端：已移除 `X-UID` 注入与临时头，`axios` 保留 `withCredentials: true`；后续完善 401 登录引导与 429/5xx 统一 Toast。

- **[步骤6 余项]**
  - 将页面类 CSP 迁至 `apps/web/next.config.js` 的 `headers()`，按域名/资产白名单收敛，完成后勾选 6.52。

- **[维护与收敛]**
  - 定期扫描 404/未使用代码路径（见 5.48）。
  - 收紧 ESLint/CI（见 8）：后端与 `libs/shared` 启用 type-aware 规则；CI 加 type-check 与 lint gate。
