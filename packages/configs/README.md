# packages/configs

该目录用于集中存放可复用的工具/配置（ESLint、Prettier、TS、Jest、Turbo、Commitlint 等）的共享包与预设。

当前暂未启用具体配置包，后续可逐步迁移根配置到独立包，便于多仓复用与版本化发布。

建议后续规划：

- eslint-config-relax-git
- prettier-config-relax-git
- tsconfig-relax-git
- jest-preset-relax-git
