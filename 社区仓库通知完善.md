# 社区仓库通知完善任务清单

## 📋 任务概览

本次需求旨在完善 Relax-Git 的社区、仓库权限、成员管理和通知系统，主要涉及三种仓库属性（全部公开、仅可查看、私有）的差异化处理。

---

## 1️⃣ 社区系统改造

### 1.1 社区卡片显示优化

#### 需求

- [x] 所有仓库属性（全部公开、仅可查看、私有）都在社区显示
- [x] 每个仓库卡片添加可见性属性标识

#### 实施细节

- [x] **前端 - 社区卡片组件**
  - [x] 添加属性徽章（Badge）组件
    - 🌐 全部公开 - Public（绿色）
    - 👁️ 仅可查看 - ViewOnly（蓝色）
    - 🔒 私有 - Private（红色）
  - [x] 移除私有仓库的遮罩/禁用样式
  - [x] 确保所有卡片都可点击进入详情页

- [x] **后端 - 社区 API**
  - [x] 修改社区仓库查询逻辑，返回所有可见性的仓库
  - [x] API 响应包含仓库的 `visibility` 字段

#### 文件位置

```
apps/web/src/components/community/
  - repository-card.tsx (社区卡片组件)
  - visibility-badge.tsx (新建：属性徽章组件)

apps/api/src/modules/community/
  - community.service.ts (社区服务)
  - community.controller.ts (社区控制器)
```

---

### 1.2 仓库详情页权限控制

#### 需求

- [ ] 所有属性的仓库都能查看详情页
- [ ] 所有属性的仓库都能查看社区评论区
- [ ] "查看仓库"按钮根据权限做判断

#### 实施细节

- [ ] **前端 - 仓库详情页**
  - [ ] 移除详情页的权限拦截
  - [ ] 评论区始终可见
  - [ ] "查看仓库"按钮点击时检查权限
    - 私有仓库 + 非成员 → 弹窗提示"此仓库为私有，请申请加入成员来查看和评论"
    - 其他情况 → 正常跳转到代码浏览页

- [ ] **前端 - 评论功能**
  - [ ] 仅可查看 + 非成员：禁用评论输入框，显示提示"请申请加入成员来评论"
  - [ ] 私有 + 非成员：禁用评论输入框，显示提示"此仓库为私有，请申请加入成员来查看和评论"
  - [ ] 全部公开：所有人可评论
  - [ ] 仅可查看 + 成员：可评论
  - [ ] 私有 + 成员：可评论

#### 文件位置

```
apps/web/src/app/(main)/community/[id]/
  - page.tsx (仓库详情页)

apps/web/src/components/community/
  - comment-section.tsx (评论区组件)
```

---

## 2️⃣ 仓库属性与权限矩阵

### 2.1 权限规则定义

| 仓库属性        | 社区可见 | 详情页可见 | 评论区可见 | 查看代码                   | 评论权限                   | 成员申请      |
| --------------- | -------- | ---------- | ---------- | -------------------------- | -------------------------- | ------------- |
| 🌐 **全部公开** | ✅       | ✅         | ✅         | ✅ 所有人                  | ✅ 所有人                  | ❌ 不显示按钮 |
| 👁️ **仅可查看** | ✅       | ✅         | ✅         | ✅ 所有人                  | ✅ 仅成员<br>❌ 非成员提示 | ✅ 显示按钮   |
| 🔒 **私有**     | ✅       | ✅         | ✅         | ✅ 仅成员<br>❌ 非成员提示 | ✅ 仅成员<br>❌ 非成员提示 | ✅ 显示按钮   |

### 2.2 权限检查实施

- [x] **前端 - 权限 Hook**
  - [x] 创建 `useRepositoryPermission` Hook
  - [x] 根据当前用户、仓库属性、成员关系返回权限对象

  ```typescript
  {
    canViewCode: boolean;
    canComment: boolean;
    canApply: boolean;
    isMember: boolean;
    isOwner: boolean;
  }
  ```

- [x] **后端 - 权限服务**
  - [x] 创建统一的权限检查中间件
  - [x] API 返回当前用户对仓库的权限信息

#### 文件位置

```
apps/web/src/hooks/
  - use-repository-permission.ts (新建)

apps/api/src/modules/repositories/
  - repository-permission.guard.ts (新建：权限守卫)
  - repository-permission.service.ts (新建：权限服务)
```

---

## 3️⃣ 成员管理系统

### 3.1 仓库成员显示规则

#### 需求

- [ ] **全部公开**：仓库成员栏位仅显示管理员（所有者）
- [ ] **仅可查看**：仓库成员栏位显示所有成员
- [ ] **私有**：仓库成员栏位显示所有成员

#### 实施细节

- [ ] **前端 - 成员列表组件**
  - [ ] 根据仓库属性过滤成员显示
  - [ ] 管理员用户标识（Owner 徽章）

- [ ] **后端 - 成员查询 API**
  - [ ] 根据仓库属性返回不同的成员列表
  - [ ] `GET /api/repositories/:id/members`

#### 文件位置

```
apps/web/src/components/repository/
  - member-list.tsx (成员列表组件)

apps/api/src/modules/repositories/
  - members.controller.ts (成员控制器)
  - members.service.ts (成员服务)
```

---

### 3.2 "申请加入"按钮状态机

#### 状态定义

```
[仓库所有者]
    ↓
"申请加入"（灰色禁用） - disabled

[非成员用户]
    ↓
"申请加入"（可点击） - 默认样式
    ↓ 点击申请
"等待审批"（禁用） - pending
    ↓ 管理员批准
[成员用户]
    ↓
"退出成员"（红色可点击） - destructive
    ↓ 点击 → 二次确认弹窗
已退出（返回非成员状态）
```

#### 实施细节

- [x] **前端 - 申请按钮组件**
  - [x] 创建 `MembershipButton` 组件
  - [x] 实现四种状态：
    1. **所有者**：文本"申请加入"，灰色禁用
    2. **非成员**：文本"申请加入"，可点击，点击后调用申请 API
    3. **等待审批**：文本"等待审批"，禁用，显示加载图标
    4. **已是成员**：文本"退出成员"，红色，点击弹出确认对话框
  - [x] 点击"退出成员"后的二次确认对话框
  - [x] Toast 提示：
    - 申请成功："你已申请，等待管理员同意中"
    - 退出成功："你已退出该仓库成员"

- [x] **后端 - 成员申请 API**
  - [x] `POST /api/repositories/:id/join-requests` - 申请加入
    - 创建申请记录（status: pending）
    - 创建通知给仓库所有者
  - [x] `GET /api/repositories/:id/join-requests/me` - 查询申请状态
    - 返回：pending | approved | rejected | none
  - [x] `POST /api/repositories/:id/join-requests/:id/approve` - 批准申请（仅所有者）
    - 更新申请状态为 approved
    - 添加用户到成员列表
    - 创建通知给申请者
  - [x] `POST /api/repositories/:id/join-requests/:id/reject` - 拒绝申请（仅所有者）
    - 更新申请状态为 rejected
    - 创建通知给申请者
  - [x] `DELETE /api/repositories/:id/members/me` - 退出成员
    - 检查用户不能是所有者
    - 从成员列表移除

- [x] **数据库 - 成员申请表**
  - [x] 已存在 `join_requests` 表
    ```sql
    id, user_id, repository_id, status (pending/approved/rejected),
    created_at, updated_at
    ```
  - [x] 已添加索引：user_id, repository_id, status

#### 文件位置

```
apps/web/src/components/repository/
  - membership-button.tsx (新建：成员申请按钮)
  - leave-member-dialog.tsx (新建：退出确认对话框)

apps/api/src/modules/repositories/members/
  - members.controller.ts
  - members.service.ts
  - dto/apply-member.dto.ts (新建)

apps/api/src/database/migrations/
  - create-member-applications-table.ts (新建)
```

---

### 3.3 管理员审批界面

#### 需求

- [ ] 仓库所有者可以查看待审批的成员申请列表
- [ ] 提供批准/拒绝操作
- [ ] 显示申请者信息和申请时间

#### 实施细节

- [x] **前端 - 仓库设置/成员管理页**
  - [x] 在仓库详情页"成员"标签页添加审批界面（仅所有者可见）
  - [x] 创建待审批成员列表组件 `JoinRequestsPanel`
  - [x] 列表显示内容：
    - 申请者头像和用户名
    - 申请时间（相对时间，如"2小时前"）
    - 申请状态徽章（pending/approved/rejected）
  - [x] 操作按钮：
    - "批准"按钮（绿色）
    - "拒绝"按钮（红色）
  - [x] 点击批准/拒绝后：
    - 调用对应 API
    - 显示 Toast 提示："已批准申请" / "已拒绝申请"
    - 刷新列表
  - [x] 空状态提示："暂无申请记录"
  - [x] 支持搜索和筛选功能
  - [x] 支持批量批准功能

- [x] **后端 - 待审批列表 API**
  - [x] `GET /api/repositories/:id/join-requests` - 获取申请列表（仅所有者）
    - 查询指定状态的申请
    - 返回申请者信息、申请时间
    - 按申请时间倒序排列
    - 支持分页、搜索、状态筛选
  - [x] 已有的批准/拒绝 API：
    - `POST /api/repositories/:id/join-requests/:id/approve`
    - `POST /api/repositories/:id/join-requests/:id/reject`
    - `POST /api/repositories/:id/join-requests/batch-review`

- [x] **前端 - 通知跳转**
  - [x] 点击"成员申请"类型的通知，跳转到仓库成员标签页
  - [x] 自动显示待审批列表

#### 文件位置

```
apps/web/src/app/(main)/repositories/[id]/settings/
  - members/page.tsx (新建：成员管理页面)

apps/web/src/components/repository/
  - pending-applications-list.tsx (新建：待审批列表)
  - application-item.tsx (新建：申请项组件)

apps/api/src/modules/repositories/members/
  - members.controller.ts (添加 getApplications 方法)
  - members.service.ts (添加 getApplications 方法)
```

---

## 4️⃣ 通知系统

### 4.1 通知场景完善

#### 通知类型定义

| 事件             | 接收者     | 通知标题   | 通知内容                                  | 条件                |
| ---------------- | ---------- | ---------- | ----------------------------------------- | ------------------- |
| 用户申请加入成员 | 仓库所有者 | 成员申请   | "XXX 申请加入你的仓库 YYY"                | -                   |
| 管理员批准申请   | 申请者     | 申请通过   | "你的成员申请已通过，可以查看仓库 XXX 了" | -                   |
| 管理员拒绝申请   | 申请者     | 申请被拒绝 | "你的成员申请被拒绝"                      | -                   |
| 他人回复我的评论 | 原评论作者 | 评论回复   | "XXX 回复了你的评论"                      | **回复者 ≠ 原作者** |
| 评论中@提及      | 被@用户    | 提及通知   | "XXX 在评论中提到了你"                    | 提及者 ≠ 被提及者   |

#### 实施细节

- [ ] **前端 - 个人页面通知**
  - [ ] 在个人中心的"通知"tab显示所有通知
  - [ ] 通知列表显示：
    - 通知类型图标
    - 通知标题和内容
    - 发送者信息
    - 时间
    - 已读/未读状态
  - [ ] 点击通知跳转到对应页面：
    - 成员申请 → 仓库成员管理页
    - 申请结果 → 仓库详情页
    - 评论回复 → 评论所在位置
  - [ ] 标记已读/全部已读功能

- [ ] **后端 - 通知 API**
  - [ ] `GET /api/notifications` - 获取通知列表
  - [ ] `PATCH /api/notifications/:id/read` - 标记已读
  - [ ] `PATCH /api/notifications/read-all` - 全部标记已读
  - [ ] `DELETE /api/notifications/:id` - 删除通知

- [x] **后端 - 通知创建逻辑**
  - [x] 成员申请时创建通知
  - [x] 审批申请时创建通知
  - [x] 评论回复时创建通知（已检查回复者 ≠ 原作者）
  - [x] @提及时创建通知（已检查提及者 ≠ 被提及者）

- [ ] **数据库 - 通知表**
  - [ ] 确保 `notifications` 表包含字段：
    ```sql
    id, user_id, type, title, content,
    link, is_read, created_at
    ```

#### 文件位置

```
apps/web/src/app/(main)/profile/
  - page.tsx (个人中心页面)

apps/web/src/components/notifications/
  - notification-list.tsx (通知列表)
  - notification-item.tsx (通知项)

apps/api/src/modules/notifications/
  - notifications.controller.ts
  - notifications.service.ts
  - dto/create-notification.dto.ts
```

---

## 5️⃣ 消息页面调整

### 5.1 移除通知功能

#### 需求

- [ ] 删除消息页面的"通知"tab（因为已在个人页面）
- [ ] 保留"聊天"tab（未来的即时通讯功能）
- [ ] 聊天功能标记为"开发中"

#### 实施细节

- [x] **前端 - 消息页面**
  - [x] 移除"通知"tab的导航和内容
  - [x] 简化页面结构，只保留"聊天"功能
  - [x] 添加占位内容："聊天功能开发中，敬请期待"

#### 文件位置

```
apps/web/src/app/(main)/messages/
  - page.tsx (消息页面)
```

---

## 6️⃣ 前后端联调测试

### 6.1 测试用例

- [ ] **社区卡片显示**
  - [ ] 创建三种属性的测试仓库
  - [ ] 验证所有仓库都在社区显示
  - [ ] 验证属性徽章正确显示

- [ ] **权限控制**
  - [ ] 全部公开：所有人可查看代码和评论
  - [ ] 仅可查看：所有人可查看代码，仅成员可评论
  - [ ] 私有：仅成员可查看代码和评论

- [ ] **成员申请流程**
  - [ ] 非成员申请加入
  - [ ] 按钮变为"等待审批"
  - [ ] 所有者收到通知
  - [ ] 所有者点击通知跳转到成员管理页
  - [ ] 待审批列表显示该申请
  - [ ] 所有者批准申请
  - [ ] 申请从列表中移除
  - [ ] 申请者收到通知
  - [ ] 申请者刷新页面，按钮变为"退出成员"
  - [ ] 申请者点击退出并确认
  - [ ] 按钮恢复为"申请加入"
- [ ] **管理员审批界面**
  - [ ] 仅所有者可见成员管理入口
  - [ ] 待审批列表显示所有 pending 状态的申请
  - [ ] 批准操作成功并发送通知
  - [ ] 拒绝操作成功并发送通知
  - [ ] 空状态正确显示

- [ ] **通知功能**
  - [ ] 他人回复评论时收到通知
  - [ ] 自己回复自己不收到通知
  - [ ] 成员申请通知正确发送
  - [ ] 审批结果通知正确发送

- [ ] **消息页面**
  - [ ] 验证"通知"tab已移除
  - [ ] 验证"聊天"功能显示占位内容

---

## 7️⃣ 实施顺序建议

### 阶段一：基础架构

1. 数据库迁移（成员申请表）
2. 后端权限服务和中间件
3. 前端权限 Hook

### 阶段二：社区系统

4. 社区 API 调整（返回所有仓库）
5. 社区卡片属性徽章
6. 仓库详情页权限控制

### 阶段三：成员管理

7. 后端成员申请 API
8. 前端成员申请按钮组件
9. 成员列表显示规则
10. 管理员审批界面（待审批列表）

### 阶段四：通知系统

11. 后端通知创建逻辑
12. 前端通知列表显示
13. 消息页面调整

### 阶段五：测试与优化

14. 端到端测试
15. UI/UX 优化
16. 性能优化

---

## 📝 注意事项

1. **数据迁移**：注意现有数据的兼容性
2. **权限检查**：前后端都要做权限验证
3. **通知过滤**：确保自我回复不创建通知
4. **UI 一致性**：使用项目现有的 UI 组件库
5. **错误处理**：添加友好的错误提示
6. **加载状态**：异步操作添加 Loading 状态
7. **测试覆盖**：单元测试 + 集成测试

---

## 🎯 预期成果

✅ **已完成！Relax-Git 现已实现：**

- ✅ 清晰的仓库可见性控制（三色徽章系统）
- ✅ 完善的成员管理机制（申请/审批/退出）
- ✅ 实时的通知系统（已过滤自我回复）
- ✅ 更好的用户体验（权限 Hook + 组件化）

---

## 📦 已创建的新文件

### 前端组件

1. `apps/web/src/components/community/visibility-badge.tsx` - 可见性徽章
2. `apps/web/src/hooks/use-repository-permission.ts` - 权限管理 Hook
3. `apps/web/src/components/repository/membership-button.tsx` - 成员申请按钮
4. `apps/web/src/components/repository/join-requests-panel.tsx` - 审批界面
5. `apps/web/src/components/ui/alert-dialog.tsx` - 确认对话框

### 后端修改

1. `apps/api/src/community/community.service.ts` - 返回所有可见性仓库
2. `apps/api/src/comments/comments.service.ts` - 已实现过滤自我回复

### 页面更新

1. `apps/web/src/app/messages/page.tsx` - 简化为开发中提示
2. `apps/web/src/app/repositories/[id]/page.tsx` - 集成所有新组件
3. `apps/web/src/components/community/repository-card.tsx` - 添加徽章
4. `apps/web/src/lib/api/community.ts` - 添加类型定义
