# 社区功能修复方案（任务清单）

- 适用范围：[apps/api/src/community/](cci:7://file:///f:/relax-git/apps/api/src/community:0:0-0:0)、[apps/api/src/comments/](cci:7://file:///f:/relax-git/apps/api/src/comments:0:0-0:0)、[apps/web/src/lib/api/community.ts](cci:7://file:///f:/relax-git/apps/web/src/lib/api/community.ts:0:0-0:0)、`apps/web/src/components/community/*`、[apps/web/src/app/community/page.tsx](cci:7://file:///f:/relax-git/apps/web/src/app/community/page.tsx:0:0-0:0)
- 目标：修复“仓库评论/点赞/收藏/浏览/Feed”与数据库 Schema 的偏差，统一后端接口与前端调用，保证排序/分页一致性与并发一致性。

## P0 必做项

- [x] 统一“仓库评论”到 PROJECT/快照模型，移除错误的 REPOSITORY/anchorKey 实现
  - 后端
    - 在
      [apps/api/src/community/repository-interaction.service.ts](cci:7://file:///f:/relax-git/apps/api/src/community/repository-interaction.service.ts:0:0-0:0)：
      - 移除/废弃基于 `anchorType: 'REPOSITORY'` 与 `anchorKey` 的实现。
      - 改为 `CommentAnchorType.PROJECT`，并通过 `BaseSnapshot` 关联：选择该仓库任一有效
        `baseSnapshot`（最新一个即可）。
    - 在
      [apps/api/src/community/community.service.ts](cci:7://file:///f:/relax-git/apps/api/src/community/community.service.ts:0:0-0:0)
      的评论实现路径中统一到上述逻辑（若保留该文件的评论接口）。
    - 控制器只保留一套端点，推荐使用：
      - `GET /api/community/repositories/:id/comments`（标记 `@Public()`）
      - `POST /api/community/repositories/:id/comments`
      - `DELETE /api/community/repositories/:id/comments/:commentId`
    - 为兼容前端，保留 `/api/repositories/:id/comments`
      为临时别名路由，转发到社区端点，并打印弃用日志，后续一个版本移除。
  - 前端
    - [apps/web/src/lib/api/community.ts](cci:7://file:///f:/relax-git/apps/web/src/lib/api/community.ts:0:0-0:0)
      - 将评论相关请求切换到 `/api/community/repositories/:id/comments` 与
        `/api/community/repositories/comments/:commentId/like`。
    - [apps/web/src/components/community/repository-comments.tsx](cci:7://file:///f:/relax-git/apps/web/src/components/community/repository-comments.tsx:0:0-0:0)
      - 无需变更结构，仅更新 API 调用路径。

- [x] 修复与 Prisma Schema 不匹配的问题
  - `baseSnapshot` 替代不存在的 `snapshot` 模型调用
    - 例如
      [CommunityService.createRepositoryComment()](cci:1://file:///f:/relax-git/apps/api/src/community/community.service.ts:383:2-484:3)
      中的 `this.prisma.snapshot.findFirst(...)` 改为 `this.prisma.baseSnapshot.findFirst(...)`。
  - `repoId` 替代不存在的 `repositoryId` 字段
    - 例如
      [getRepositoryComments()](cci:1://file:///f:/relax-git/apps/web/src/lib/api/community.ts:206:2-222:3)
      中 `snapshot: { repositoryId: repoId }` 改为 `snapshot: { repoId }`。
  - 删除对不存在列的写入
    - `Comment.deletedAt` 不存在，删除相关写入。
    - `Comment.likesCount` 不存在，改为使用关系计数：`_count: { likes: true }` 或额外 `count()`
      查询。
  - 评论“热门”排序
    - 使用关系计数排序：`orderBy: { likes: { _count: 'desc' } }`，并以 `createdAt desc`/`id desc`
      作为稳定的次序键。

- [x] 分页与排序一致性（Feed 与评论列表）
  - Feed：[getCommunityFeed()](cci:1://file:///f:/relax-git/apps/api/src/community/community.service.ts:62:2-172:3)
    统一为稳定排序与光标规则
    - latest：`orderBy: [{ createdAt: 'desc' }, { id: 'desc' }]`，cursor 使用 `(createdAt, id)`
      复合条件：`where: createdAt < last.createdAt OR (createdAt = last.createdAt AND id < last.id)`。
    - popular：`orderBy: [{ stars: 'desc' }, { id: 'desc' }]`；cursor 按 `stars,id`
      复合条件或退化为 offset（短期可接受）。
    - trending：`orderBy: [{ trendingScore: 'desc' }, { id: 'desc' }]`；cursor 同上（或短期用 offset）。
  - 评论列表：与 latest 方案一致，按 `createdAt desc, id desc` + 复合条件游标。

- [x] 点赞/收藏并发一致性与缓存回填
  - [RepositoryInteractionService.toggleRepositoryLike()](cci:1://file:///f:/relax-git/apps/api/src/community/repository-interaction.service.ts:25:2-91:3)
    与
    [toggleRepositoryCollection()](cci:1://file:///f:/relax-git/apps/web/src/lib/api/community.ts:195:2-204:3)
    - 使用 `prisma.$transaction([])` 将增删与统计更新打包，避免竞态。
    - `Repository.stars` 推荐以 `RepositoryLike.count({repoId})` 实时回填，或在事务内严格增减。
  - 评论点赞
    [toggleCommentLike()](cci:1://file:///f:/relax-git/apps/web/src/lib/api/community.ts:238:2-249:3)：移除对不存在的
    `likesCount` 列增减，使用 `CommentLike` 计数后返回。

- [x] 匿名读取评论与前端 401 兜底
  - 在最终保留的 GET 评论端点上标注
    `@Public()`（[apps/api/src/community/community.controller.ts](cci:7://file:///f:/relax-git/apps/api/src/community/community.controller.ts:0:0-0:0)）。
  - 前端
    [CommunityAPI.getRepositoryComments()](cci:1://file:///f:/relax-git/apps/web/src/lib/api/community.ts:206:2-222:3)
    增加与 Feed 相同的 401 匿名重试逻辑。

## P1 改进项

- [x] 浏览统计真实客户端 IP
  - `apps/api/src/main.ts` 信任代理：`app.set('trust proxy', 1)`。
  - [recordRepositoryView()](cci:1://file:///f:/relax-git/apps/web/src/lib/api/community.ts:251:2-278:3)
    优先读取 `X-Forwarded-For`，回落到 `req.ip`。
  - 保持 1 小时幂等去重规则。

- [x] 热门标签统计由 DB 进行聚合
  - 现状从 DB 拉全量再内存聚合。
  - 方案：使用 Prisma `groupBy` 或引入标签表/索引（PostgreSQL GIN/Trigram），避免全表扫描。

- [x] 后端搜索对接前端 filters.search
  - Feed 增加 `name ILIKE`、`description ILIKE`、`tags has`
    的过滤逻辑（大小写不敏感，tags 做等值匹配）。
  - 前端保留现有参数传递。

- [x] 前端体验与一致性
  - [repository-card.tsx](cci:7://file:///f:/relax-git/apps/web/src/components/community/repository-card.tsx:0:0-0:0)
    评论数显示真实值（后端返回聚合计数或额外接口）。
  - 浏览数自增仅保留一次（曝光或点击二选一），避免 UI 双计。

- [x] 文档与迁移
  - 在接口说明中标注 `/api/repositories/:id/comments` 已弃用，迁移至
    `/api/community/repositories/:id/comments`。
  - 保留一版兼容别名后移除。
  - 新增文档：`docs/api/community-deprecation.md`。

## P2 保障项

- [ ] 定时任务运行保障
  - 确认根模块启用了 `ScheduleModule.forRoot()`，确保
    [views-aggregation.service.ts](cci:7://file:///f:/relax-git/apps/api/src/community/views-aggregation.service.ts:0:0-0:0)
    的 `@Cron` 正常触发。

- [ ] 回归与端到端用例
  - 场景覆盖：Feed（三种排序+筛选）、仓库详情、评论增删改查/点赞、仓库点赞/收藏、浏览统计、热门标签/语言。
  - 最少一条匿名路径用例（Feed/评论列表应匿名可用）。

## 受影响文件（关键）

- 后端（社区）
  - [apps/api/src/community/community.controller.ts](cci:7://file:///f:/relax-git/apps/api/src/community/community.controller.ts:0:0-0:0)
  - [apps/api/src/community/community.service.ts](cci:7://file:///f:/relax-git/apps/api/src/community/community.service.ts:0:0-0:0)
  - [apps/api/src/community/repository-interaction.controller.ts](cci:7://file:///f:/relax-git/apps/api/src/community/repository-interaction.controller.ts:0:0-0:0)
  - [apps/api/src/community/repository-interaction.service.ts](cci:7://file:///f:/relax-git/apps/api/src/community/repository-interaction.service.ts:0:0-0:0)
  - [apps/api/src/community/views-aggregation.service.ts](cci:7://file:///f:/relax-git/apps/api/src/community/views-aggregation.service.ts:0:0-0:0)
- 后端（评论公共模块，用于参考/对齐）
  - [apps/api/src/comments/comments.service.ts](cci:7://file:///f:/relax-git/apps/api/src/comments/comments.service.ts:0:0-0:0)
- 前端
  - [apps/web/src/lib/api/community.ts](cci:7://file:///f:/relax-git/apps/web/src/lib/api/community.ts:0:0-0:0)
  - [apps/web/src/components/community/repository-comments.tsx](cci:7://file:///f:/relax-git/apps/web/src/components/community/repository-comments.tsx:0:0-0:0)
  - [apps/web/src/components/community/repository-card.tsx](cci:7://file:///f:/relax-git/apps/web/src/components/community/repository-card.tsx:0:0-0:0)
  - [apps/web/src/components/community/repository-detail-modal.tsx](cci:7://file:///f:/relax-git/apps/web/src/components/community/repository-detail-modal.tsx:0:0-0:0)
  - [apps/web/src/app/community/page.tsx](cci:7://file:///f:/relax-git/apps/web/src/app/community/page.tsx:0:0-0:0)

## 交付内容（本次完成项）

- **[核心改动摘要]**
  - 统一项目级评论到 `CommentAnchorType.PROJECT` + `BaseSnapshot`，移除 `REPOSITORY/anchorKey`
    路径。
  - 修复 Prisma 调用与字段：`snapshot -> baseSnapshot`，`repositoryId -> repoId`；删除对不存在的
    `deletedAt/likesCount` 的读写。
  - Feed 与评论列表采用稳定排序与复合游标：
    - Feed：`latest (createdAt,id)`、`popular (stars,id)`、`trending (trendingScore,id)`；游标为 base64
      JSON。
    - 评论：`latest/oldest` 使用 `(createdAt,id)`；`popular` 按 `_count(likes)`
      排序（短期游标退化）。
  - 点赞/收藏操作使用 `prisma.$transaction` 并通过 `count()` 回填，避免并发漂移；评论点赞返回
    `CommentLike.count()`。
  - 匿名访问：评论列表与仓库详情标注 `@Public()`；前端获取评论加入 401 匿名重试。
  - 旧端点兼容与弃用日志：`/api/repositories/...` 转发到 `/api/community/...`
    并打印 warn（保留一版）。
  - 浏览统计优先读取 `X-Forwarded-For`（真实 IP），回落 `req.ip`（幂等去重仍为 1 小时）。

- **[端点清单（社区保留）]**
  - `GET /api/community/feed`
  - `GET /api/community/repositories/:id`（Public）
  - `POST /api/community/repositories/:id/view`（Public）
  - `GET /api/community/repositories/:id/comments`（Public）
  - `POST /api/community/repositories/:id/comments`
  - `DELETE /api/community/repositories/:id/comments/:commentId`
  - `POST /api/community/repositories/comments/:commentId/like`
  - 兼容别名（已打日志，后续下线）：
    - `GET/POST /api/repositories/:id/comments`
    - `POST /api/repositories/comments/:commentId/like`

- **[主要代码改动文件]**
  - 后端：
    - `apps/api/src/community/community.service.ts`
    - `apps/api/src/community/community.controller.ts`
    - `apps/api/src/community/repository-interaction.service.ts`
    - `apps/api/src/community/repository-interaction.controller.ts`
  - 前端：
    - `apps/web/src/lib/api/community.ts`

- **[验证步骤]**
  - 打开前端“社区”页，匿名态应能加载 Feed、仓库详情与评论列表；分页无重复/遗漏。
  - 登录后：
    - 点赞/取消点赞仓库，stars 数正确；高并发点击计数不漂移。
    - 收藏/取消收藏仓库，collections 计数正确。
    - 发表评论/回复评论成功，无 `anchorKey/snapshot/likesCount` 等错误。
    - 点赞/取消点赞评论，返回 `isLiked/likesCount` 正确。
  - 浏览统计：卡片曝光或详情点击仅记一次；服务端记录 IP 优先 `X-Forwarded-For`，1 小时去重生效。

- **[回滚与注意事项]**
  - 前端可临时切回旧端点 `/api/repositories/...`（仍保留兼容，控制器已转发并打印弃用日志）。
  - 评论“popular”游标暂为退化实现，若遇到边界重复可临时改回 offset；推荐尽快完善基于 `_count`
    的复合游标策略。
  - 生产环境请在反向代理层设置并透传 `X-Forwarded-For`；Fastify 的 trust
    proxy 策略需按部署方式另行配置（见 P1）。

## 验收标准

- [ ] 前端“社区”页可匿名加载 Feed、仓库详情与评论列表（登录后具备点赞/收藏/发表评论等）。
- [ ] 评论创建、热门排序、删除、点赞不再报错（无 likesCount/anchorKey/snapshot 模型错误）。
- [ ] Feed 与评论的分页不出现重复/遗漏。
- [ ] 点赞/收藏在并发下无计数漂移。
- [ ] 浏览统计按 1 小时去重，聚合与热度分数任务正常运行。
- [ ] 旧端点访问日志打印弃用提醒，一个版本后下线。

## 风险与回滚

- 风险：接口切换期间前端/后端不一致；分页逻辑变更引入边界问题。
- 回滚：保留旧端点别名与特性开关，若出问题立刻回切旧端点并记录日志；分页可临时退回 offset 模式（短名单页）。
